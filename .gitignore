node_modules/
dist/
.next/
.turbo/
.env
*.tsbuildinfo

# User-created actions
packages/core/src/actions/user/
/actions/

# Data directory
data/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.claude/work-log/

# graphify knowledge graph — graph.json is committed so it survives worktree
# removal and lands in fresh worktrees; everything else is per-machine or
# regenerable. Merges use the union driver (see .gitattributes).
graphify-out/*
!graphify-out/graph.json
